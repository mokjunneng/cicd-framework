name: "go-test"
description: "testing for GO"
inputs:
  # Path/s to execute test
  path:
    required: true
outputs:
runs:
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/golint.sh ${{ inputs.path }}
      shell: bash
# TODO: How to pipe lint output to STDOUT for us to inspect in Github console
